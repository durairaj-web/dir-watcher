'use strict'

const path = require('path'); // Importing the path module

// Exporting a configuration object with the path to the config file
module.exports = {
    'config': path.resolve('config', 'config.js') // Resolving the path to the config file
};
