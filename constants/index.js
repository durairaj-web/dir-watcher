'use strict'

const messageTxt = require('./message');
const errorCode = require('./errorCode');
const statusCode = require('./statusCode');

module.exports = {
    messageTxt,
    errorCode,
    statusCode
}